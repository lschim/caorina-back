services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: caorina
      POSTGRES_USER: caorina
      POSTGRES_PASSWORD: caorina
    ports:
      - "54320:5432"
    volumes:
      - caorina-data:/var/lib/postgresql/data

volumes:
  caorina-data:
